<Window x:Class="PatientConsentHub.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Patient Consent Hub - Sign in"
        Height="470" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{StaticResource PageBackgroundBrush}"
        FontFamily="{StaticResource UiFont}">

    <Border Margin="28" Style="{StaticResource Card}" Padding="30">
        <StackPanel>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,4,0,18">
                <ContentControl Style="{StaticResource ATLogoStyle}" Width="52" Height="52" />
                <StackPanel VerticalAlignment="Center" Margin="14,0,0,0">
                    <TextBlock Text="Patient Consent Hub" Style="{StaticResource TitleText}" FontSize="20" />
                    <TextBlock Text="by A&amp;T" Style="{StaticResource SubtitleText}" />
                </StackPanel>
            </StackPanel>

            <Border Height="1" Background="{StaticResource BorderBrush}" Margin="0,0,0,20" />

            <TextBlock Text="Username" Style="{StaticResource FieldLabel}" />
            <TextBox x:Name="UsernameBox" Style="{StaticResource BigTextBox}" Margin="0,0,0,16" />

            <TextBlock Text="Password" Style="{StaticResource FieldLabel}" />
            <PasswordBox x:Name="PasswordBox" Style="{StaticResource BigPasswordBox}" Margin="0,0,0,8" />

            <TextBlock x:Name="MessageText"
                       Foreground="{StaticResource RecordBrush}"
                       FontSize="13"
                       TextWrapping="Wrap"
                       Visibility="Collapsed"
                       Margin="0,4,0,8" />

            <Button x:Name="SignInButton"
                    Content="SIGN IN"
                    Style="{StaticResource PrimaryButton}"
                    Height="52" FontSize="17"
                    Margin="0,12,0,0"
                    IsDefault="True"
                    Click="OnSignInClick" />

            <TextBlock x:Name="ModeText"
                       Style="{StaticResource SubtitleText}"
                       HorizontalAlignment="Center"
                       Margin="0,18,0,0" />
        </StackPanel>
    </Border>
</Window>
